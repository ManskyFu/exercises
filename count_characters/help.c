#include "count.h"
#include <stdio.h>

void help(void)
{
    puts(" -h            show help information.");
    puts(" -i            get strings from keyboard.");
    puts(" -f filename   get strings from a file.");
}
